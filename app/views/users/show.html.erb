<%= render 'layouts/navbar' %>

<div class="container">
  <div class="row profile">
    <div class="col-md-3">
      <div class="profile-sidebar">
        <!-- SIDEBAR USERPIC -->
        <div class="profile-userpic">
          <%= image_tag @user.avatar.url(:large),:class => 'img-responsive'  %>
        </div>
        <!-- END SIDEBAR USERPIC -->
        <!-- SIDEBAR USER TITLE -->
        <div class="profile-usertitle">
          <div class="profile-usertitle-name">
            <%= @user.username %>
          </div>
          <div class="profile-usertitle-motto">
            <%= @user.motto %>
          </div>
        </div>
        <!-- END SIDEBAR USER TITLE -->
        <!-- SIDEBAR BUTTONS -->
        <div class="profile-userbuttons">
          <% if @user == current_user %>
            <%= link_to mailbox_inbox_path, method: 'get', class: 'btn btn-danger btn-sm', style: 'background: #233E47;border: none;' do %>
            inbox
            <% if unread_messages_count > 0 %>
              <span class="label label-danger"><%=unread_messages_count%></span>
            <% end %>
          <% end %>

          <% else %>
            <%= link_to "Message", new_conversation_path(user: @user.username), method: :get, class: 'btn btn-danger btn-sm', style: 'background: #233E47;border: none;' %>
          <% end %>
        </div>
        <!-- END SIDEBAR BUTTONS -->
        <!-- SIDEBAR MENU -->
        <div class="profile-usermenu">
          <ul class="nav">
            <li class="active" data-turbolinks="false">
              <a href="#">
                <i class="glyphicon glyphicon-home"></i>
                Overview </a>
            </li>
            <% if current_user == @user %>
              <li data-turbolinks="false" data-toggle="modal" data-target="#postModal">
                <a href="#">
                  <i class="glyphicon glyphicon-camera"></i>
                  Add Post </a>
              </li>
            <% end %>
            <li data-turbolinks="false" data-toggle="modal" data-target="#spotModal">
                <a href="#">
                  <i class="glyphicon glyphicon-map-marker"></i>
                  Spots </a>
            </li>
          </ul>
        </div>
        <!-- END MENU -->
      </div>
    </div>
    <div class="col-md-9">
      <div class="profile-content">
        <div class="row">
          <% @user.posts.each do |post| %>
            <div class="col-sm-6 col-md-6" style="padding: 0px;margin-top: 10px;">
              <% if post.type == 'ImagePost' %>
                <%= image_tag post.image.url(:medium),:class => 'img-responsive' %>
              <% else %>
                <%= embed post.video_url %>
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
<br>
<br>

<%= render 'users/spots_modal' %>
<%= render 'posts/form' %>


