


<div class="meetups">
  <div class="meetupscontent">
    <h1> Meetups </h1>
    <h3> spot name </h3>
    <ul> 
      <li>time</li>
      <li>date</li>
    </ul>
    <h3> attendees </h3>
     <div class="form-group">
      <select class="form-control" id="sel1" style="width: 150px;margin: 0 auto;">
        <option>username</option>
        <option>username</option>
        <option>username</option>
        <option>username</option>
      </select>
    </div>
    <ul>
      <li><button type="button" class="btn btn-success" style="border: none;">Showing</button></li>
      <li><button type="button" class="btn btn-danger" style="border: none;">Leave</button></li>
    </ul>
    <h3> nothing planned? </h3>
    <button type="button" class="btn btn-primary btn-success btn-lg" style="width: 150px;margin: 0 auto;border: none;">Start One</button>
    <p><%= link_to ' Back ', spots_path %></p>
  </div>
</div>


<!-- reference
<p id="notice"><%= notice %></p>

<h1>Listing Events</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Time</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.title %></td>
        <td><%= event.description %></td>
        <td><%= event.datetime %></td>
        <td><%= link_to 'Show', event %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event', new_event_path %>
-->
